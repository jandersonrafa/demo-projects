services:
  prometheus:
    image: prom/prometheus:latest
    container_name: benchmark-prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--web.enable-remote-write-receiver'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
    ports:
      - "9091:9090"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 6G

  grafana:
    image: grafana/grafana:latest
    container_name: benchmark-grafana
    ports:
      - "3000:3000"
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/var/lib/grafana/dashboards
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - prometheus
    deploy:
      resources:
        limits:
          memory: 6G

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: benchmark-cadvisor
    ports:
      - "8081:8080"
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:rw
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    deploy:
      resources:
        limits:
          memory: 6G

  k6:
    image: grafana/k6:latest
    container_name: benchmark-k6
    volumes:
      - ./k6/scripts:/scripts
      - ./k6/reports:/reports
    working_dir: /scripts
    environment:
      - TARGETS=192.168.0.114:3005,192.168.0.114:3006,192.168.0.114:3007,192.168.0.114:3008,192.168.0.114:3009,192.168.0.114:3011,192.168.0.114:3014,192.168.0.114:3016
      - K6_PROMETHEUS_RW_SERVER_URL=http://prometheus:9090/api/v1/write
      - K6_PROMETHEUS_RW_TREND_STATS=p(95),p(99),avg

    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: [ "run", "-o", "experimental-prometheus-rw", "load-all.js" ]
    profiles:
      - tools
  # - TARGETS=host.docker.internal:3005,host.docker.internal:3006,host.docker.internal:3007,host.docker.internal:3008,host.docker.internal:3009,host.docker.internal:3011,host.docker.internal:3014,host.docker.internal:3016
  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:latest
    container_name: benchmark-postgres-exporter
    environment:
      - DATA_SOURCE_NAME=postgresql://postgres:postgres@host.docker.internal:5432/benchmark?sslmode=disable
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "9187:9187"
    deploy:
      resources:
        limits:
          memory: 512M
volumes:
  prometheus_data:
