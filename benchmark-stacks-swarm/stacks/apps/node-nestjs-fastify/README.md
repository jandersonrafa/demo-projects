Aplicação NestJS monolítica usando Fastify, baseada na app `node-nestjs-express` original, ajustada para benchmarks de performance.

## Comandos principais

```bash
npm install
npm run build
npm run start:prod
```
